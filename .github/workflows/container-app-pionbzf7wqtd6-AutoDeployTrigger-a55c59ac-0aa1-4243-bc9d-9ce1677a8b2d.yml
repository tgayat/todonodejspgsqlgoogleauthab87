name: Trigger auto deployment for container-app-pionbzf7wqtd6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-pionbzf7wqtd6-AutoDeployTrigger-a55c59ac-0aa1-4243-bc9d-9ce1677a8b2d.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPPIONBZF7WQTD6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPPIONBZF7WQTD6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPPIONBZF7WQTD6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPPIONBZF7WQTD6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPPIONBZF7WQTD6_REGISTRY_PASSWORD }}
          containerAppName: container-app-pionbzf7wqtd6
          resourceGroup: todonodejspgsqlgoogleauthab87a17f
          imageToBuild: default/container-app-pionbzf7wqtd6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


